spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ECommerce
    username: postgres
    password: "hari@2002"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    username: abhisuvarnakar@gmail.com
    password: xbra rqwv gxke fydv
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8200
  servlet:
    context-path: /notifications

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

kafka:
  topic:
    email-out: EMAILOUT